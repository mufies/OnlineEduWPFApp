<Page x:Class="OnlineEduTaskWPF.AdminDashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Admin Dashboard">

    <Grid Background="White" Margin="20">
        <StackPanel>
            <TextBlock Text="📊 Admin Dashboard" 
                      FontSize="24" 
                      FontWeight="Bold" 
                      Margin="0,0,0,20"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Statistics Cards -->
                <Border Grid.Column="0" Background="#E3F2FD" Padding="20" Margin="5" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="👨‍🎓 Total Students" FontSize="14" Foreground="#666"/>
                        <TextBlock x:Name="txtTotalStudents" Text="0" FontSize="32" FontWeight="Bold" Foreground="#1976D2"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Background="#E8F5E9" Padding="20" Margin="5" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="👨‍🏫 Total Teachers" FontSize="14" Foreground="#666"/>
                        <TextBlock x:Name="txtTotalTeachers" Text="0" FontSize="32" FontWeight="Bold" Foreground="#388E3C"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Background="#FFF3E0" Padding="20" Margin="5" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="🏫 Total Classes" FontSize="14" Foreground="#666"/>
                        <TextBlock x:Name="txtTotalClasses" Text="0" FontSize="32" FontWeight="Bold" Foreground="#F57C00"/>
                    </StackPanel>
                </Border>
            </Grid>

            <TextBlock Text="Welcome to Admin Dashboard. Use the menu on the left to manage the system." 
                      FontSize="14" 
                      Foreground="#666" 
                      Margin="0,30,0,0"/>
        </StackPanel>
    </Grid>
</Page>
