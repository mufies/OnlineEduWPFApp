<Window x:Class="OnlineEduTaskWPF.SubmitTaskDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Submit Task" 
        Height="500" 
        Width="600"
        WindowStartupLocation="CenterScreen"
        Background="White"
        ResizeMode="NoResize">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" 
                  Text="📝 Submit Assignment" 
                  FontSize="24" 
                  FontWeight="Bold"
                  Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1"
                  x:Name="txtTaskTitle"
                  FontSize="16" 
                  Foreground="#666"
                  TextWrapping="Wrap"
                  Margin="0,0,0,20"/>

        <!-- Error Message -->
        <TextBlock x:Name="txtError" 
                  Grid.Row="2"
                  Foreground="Red" 
                  FontWeight="SemiBold"
                  TextWrapping="Wrap"
                  Margin="0,0,0,10"
                  Visibility="Collapsed"
                  VerticalAlignment="Top"/>

        <!-- Submission Content -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                      Text="Your Submission *" 
                      FontWeight="SemiBold" 
                      FontSize="14"
                      Margin="0,0,0,8"/>

            <Border Grid.Row="1"
                   BorderBrush="#CCCCCC"
                   BorderThickness="1"
                   CornerRadius="4">
                <TextBox x:Name="txtSubmission" 
                        Padding="12" 
                        FontSize="14" 
                        TextWrapping="Wrap" 
                        AcceptsReturn="True"
                        VerticalScrollBarVisibility="Auto"
                        BorderThickness="0"
                        Background="White"/>
            </Border>
        </Grid>

        <!-- Info Text -->
        <TextBlock Grid.Row="3"
                  Text="Note: Once submitted, you cannot edit your submission until teacher grades it."
                  FontSize="12"
                  Foreground="#FF9800"
                  FontStyle="Italic"
                  TextWrapping="Wrap"
                  Margin="0,15,0,20"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" 
                   Orientation="Horizontal" 
                   HorizontalAlignment="Right">
            <Button Content="Submit"
                   x:Name="btnSubmit"
                   Click="btnSubmit_Click"
                   Background="#4CAF50"
                   Foreground="White"
                   Padding="30,12"
                   FontSize="15"
                   FontWeight="SemiBold"
                   BorderThickness="0"
                   Cursor="Hand"
                   Margin="0,0,10,0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#4CAF50"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45a049"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <Button Content="Cancel"
                   Click="btnCancel_Click"
                   Background="#9E9E9E"
                   Foreground="White"
                   Padding="30,12"
                   FontSize="15"
                   BorderThickness="0"
                   Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#9E9E9E"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#757575"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
