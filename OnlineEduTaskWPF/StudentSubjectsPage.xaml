<Page x:Class="OnlineEduTaskWPF.StudentSubjectsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="My Subjects"
      Background="#F5F7FA">

    <Page.Resources>
        <!-- ListBoxItem Style with Hover Effect -->
        <Style x:Key="SubjectListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="Transparent"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="Transparent"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Subject Card Style -->
        <Style x:Key="SubjectCardBorder" TargetType="Border">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#E3F2FD" Offset="0"/>
                        <GradientStop Color="#BBDEFB" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Padding" Value="25"/>
            <Setter Property="BorderBrush" Value="#90CAF9"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#2196F3" 
                                    Direction="270" 
                                    ShadowDepth="4" 
                                    Opacity="0.2" 
                                    BlurRadius="15"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#1976D2" 
                                            Direction="270" 
                                            ShadowDepth="8" 
                                            Opacity="0.4" 
                                            BlurRadius="25"/>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <TranslateTransform Y="-3"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Button Style -->
        <Style x:Key="ViewTasksButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder"
                                Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#1565C0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Modern Header with Gradient -->
        <Border Grid.Row="0" 
                Padding="40,30"
                Margin="0,0,0,20">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#1976D2" Offset="0"/>
                    <GradientStop Color="#2196F3" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect Color="#1976D2" 
                                Direction="270" 
                                ShadowDepth="3" 
                                Opacity="0.3" 
                                BlurRadius="12"/>
            </Border.Effect>
            
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="📚" 
                              FontSize="36" 
                              VerticalAlignment="Center"
                              Margin="0,0,15,0"/>
                    <TextBlock Text="My Enrolled Subjects" 
                              FontSize="32" 
                              FontWeight="Bold" 
                              Foreground="White"
                              VerticalAlignment="Center"/>
                </StackPanel>
                
                <TextBlock x:Name="txtSemesterInfo"
                          Text="Semester 1 - Academic Year 2024-2025" 
                          FontSize="15" 
                          Foreground="#E3F2FD"
                          Margin="51,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Subjects List -->
        <ScrollViewer Grid.Row="1" 
                     VerticalScrollBarVisibility="Auto"
                     Padding="40,0,40,40">
            <ListBox x:Name="lstSubjects"
                    BorderThickness="0"
                    Background="Transparent"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                    ItemContainerStyle="{StaticResource SubjectListBoxItemStyle}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource SubjectCardBorder}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Icon Circle -->
                                <Border Grid.Column="0"
                                       Background="#2196F3"
                                       Width="80"
                                       Height="80"
                                       CornerRadius="40"
                                       VerticalAlignment="Center"
                                       Margin="0,0,25,0">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#1976D2" 
                                                        Direction="270" 
                                                        ShadowDepth="3" 
                                                        Opacity="0.3" 
                                                        BlurRadius="10"/>
                                    </Border.Effect>
                                    <TextBlock Text="📖" 
                                              FontSize="40"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                </Border>

                                <!-- Subject Information -->
                                <StackPanel Grid.Column="1" 
                                           VerticalAlignment="Center"
                                           Margin="0,5,20,5">
                                    
                                    <!-- Subject Code Badge -->
                                    <Border Background="#1976D2" 
                                           CornerRadius="5" 
                                           Padding="10,5"
                                           HorizontalAlignment="Left"
                                           Margin="0,0,0,10">
                                        <TextBlock Text="{Binding SubjectCode}" 
                                                  FontSize="13" 
                                                  FontWeight="Bold"
                                                  Foreground="White"/>
                                    </Border>

                                    <!-- Subject Name -->
                                    <TextBlock Text="{Binding SubjectName}" 
                                              FontSize="22" 
                                              FontWeight="Bold"
                                              Foreground="#1565C0"
                                              TextWrapping="Wrap"
                                              Margin="0,0,0,12"/>

                                    <!-- Credits Info -->
                                    <StackPanel Orientation="Horizontal">
                                        <Border Background="#FF9800" 
                                               CornerRadius="12" 
                                               Padding="12,6">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="⭐" 
                                                          FontSize="14"
                                                          Margin="0,0,5,0"/>
                                                <TextBlock Text="{Binding Credits}" 
                                                          FontSize="15" 
                                                          FontWeight="Bold"
                                                          Foreground="White"/>
                                                <TextBlock Text=" Credits" 
                                                          FontSize="15" 
                                                          FontWeight="SemiBold"
                                                          Foreground="White"
                                                          Margin="5,0,0,0"/>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Action Button -->
                                <StackPanel Grid.Column="2" 
                                           VerticalAlignment="Center">
                                    <Button Content="View Tasks →" 
                                           Style="{StaticResource ViewTasksButton}"
                                           Tag="{Binding}"
                                           Click="btnViewTasks_Click"/>
                                    
                                    <TextBlock Text="Click to see assignments" 
                                              FontSize="11" 
                                              Foreground="#1976D2"
                                              HorizontalAlignment="Center"
                                              Margin="0,8,0,0"
                                              FontStyle="Italic"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>

        <!-- Empty State (shown when no subjects) -->
        <StackPanel Grid.Row="1"
                   x:Name="emptyState"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   Visibility="Collapsed">
            <TextBlock Text="📚" 
                      FontSize="80" 
                      HorizontalAlignment="Center"
                      Opacity="0.3"/>
            <TextBlock Text="No subjects enrolled yet" 
                      FontSize="20" 
                      FontWeight="SemiBold"
                      Foreground="#999999"
                      HorizontalAlignment="Center"
                      Margin="0,15,0,0"/>
            <TextBlock Text="Contact your advisor to enroll in courses" 
                      FontSize="14" 
                      Foreground="#BBBBBB"
                      HorizontalAlignment="Center"
                      Margin="0,8,0,0"/>
        </StackPanel>
    </Grid>
</Page>
