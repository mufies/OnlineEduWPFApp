<Page x:Class="OnlineEduTaskWPF.TeacherClassesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="My Classes">

    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                  Text="🏫 My Teaching Classes" 
                  FontSize="24" 
                  FontWeight="Bold" 
                  Margin="0,0,0,20"/>

        <DataGrid Grid.Row="1" 
                 x:Name="dgClasses" 
                 AutoGenerateColumns="False"
                 CanUserAddRows="False"
                 IsReadOnly="True"
                 GridLinesVisibility="Horizontal"
                 HeadersVisibility="Column"
                 AlternatingRowBackground="#F5F5F5"
                 BorderBrush="#DDDDDD"
                 BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Class Code" Binding="{Binding ClassCode}" Width="120"/>
                <DataGridTextColumn Header="Class Name" Binding="{Binding ClassName}" Width="*"/>
                <DataGridTextColumn Header="Subject Code" Binding="{Binding SubjectCode}" Width="100"/>
                <DataGridTextColumn Header="Subject Name" Binding="{Binding SubjectName}" Width="*"/>
                <DataGridTextColumn Header="Credits" Binding="{Binding Credits}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
