<Page x:Class="OnlineEduTaskWPF.TeacherSubjectsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="My Subjects">

    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                  Text="📚 My Teaching Subjects" 
                  FontSize="24" 
                  FontWeight="Bold" 
                  Margin="0,0,0,20"/>

        <DataGrid Grid.Row="1" 
                 x:Name="dgSubjects" 
                 AutoGenerateColumns="False"
                 CanUserAddRows="False"
                 IsReadOnly="True"
                 GridLinesVisibility="Horizontal"
                 HeadersVisibility="Column"
                 AlternatingRowBackground="#F5F5F5"
                 BorderBrush="#DDDDDD"
                 BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Subject Code" Binding="{Binding SubjectCode}" Width="120"/>
                <DataGridTextColumn Header="Subject Name" Binding="{Binding SubjectName}" Width="*"/>
                <DataGridTextColumn Header="Credits" Binding="{Binding Credits}" Width="80"/>
                <DataGridTextColumn Header="Classes Teaching" Binding="{Binding ClassCount}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
